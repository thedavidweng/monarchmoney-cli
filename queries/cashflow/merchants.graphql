query GetCashflowMerchants($filters: TransactionFilterInput) {
  aggregates(filters: $filters, groupBy: ["merchant"]) {
    groupBy {
      merchant {
        id
        name
        logoUrl
      }
    }
    summary {
      sumIncome
      sumExpense
    }
  }
}
