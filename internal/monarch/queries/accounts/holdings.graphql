query GetAccountHoldings($accountId: ID!) {
  account(id: $accountId) {
    holdings {
      id
      security {
        name
        symbol
      }
      quantity
      price
      value
    }
  }
}
