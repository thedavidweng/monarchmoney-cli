mutation SetBudget($categoryId: ID!, $amount: Float!, $month: Int, $year: Int) {
  setBudget(categoryId: $categoryId, amount: $amount, month: $month, year: $year) {
    budget {
      category {
        name
      }
      planned
    }
  }
}
